# Ashen Voice 1.4.3

- Restores the complete project structure.
- Keeps the working direct Discord connection.
- Filters Discord bots and incomplete placeholder users.
- Keeps Preview Overlay visible until Stop Preview is clicked.
- Uses unique temporary speaker-state files to reduce file-lock errors.
- Supports horizontal and vertical offsets from -10000 to 10000.
- Requests administrator privileges automatically.
- Uses a complete GitHub Actions workflow that locates the project automatically.
